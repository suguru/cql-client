{
  "framework": "mocha",
  "src_files": [
    "lib/**/*.js",
    "test/test.*.js"
  ],
  "launchers": {
    "Mocha": {
      "command": "istanbul cover node_modules/mocha/bin/_mocha --report html -- test/test.*.js -R tap",
      "protocol": "tap"
    }
  },
  "launch_in_dev": [
    "Mocha"
  ]
}
